import { PredictTrainContent } from './predict-train-content';

export const metadata = {
	title: 'Predictor Training · Interceptor',
	description: 'Seed UFC predictor training data.',
};

export default function PredictTrainPage() {
	return <PredictTrainContent />;
}
